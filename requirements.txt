# JSXRay v2.0 — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core (required) ──────────────────────────────
requests>=2.31.0          # HTTP client
Pillow>=10.0.0            # Image analysis + LSB steganography detection
jsbeautifier>=1.14.0      # JS deobfuscation / minified code beautifier

# ── Browser module (optional but recommended) ────
playwright>=1.40.0        # Chromium headless browser (SPA rendering)
# After install: playwright install chromium

# ── Extras ───────────────────────────────────────
urllib3>=1.26.0           # HTTP utilities (bundled with requests)
